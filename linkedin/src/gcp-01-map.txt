 NC2 on Google Cloud: the whole path, as code
 ============================================

 Pulumi: your project
   Compute Engine API, IP forwarding allowed, trusted images
   not blocked, vCPU + LSSD quota
   cluster VPC, MTU >= 2000
     management subnet  /24 min  (PC takes a /28 out of it)
     FVN NAT subnet     /27 min
     FVN no-NAT subnet  /27 min
     Cloud Router + Cloud NAT (BYO: dynamic ports)
   two service accounts: NC2 console, cluster nodes
        |
        v
 NC2 v2 API:  POST /clusters/gcp   -> returns a task
        |
        v
 Google Cloud region
   3 to 28 GCE bare-metal nodes, AHV + CVM + AOS
   Prism Central in its /28    <-- you talk to this
   Flow Virtual Networking overlay (no Flow Gateway VMs)
        |
        v
 Pulumi -> Prism Central v4 -> Flow VPCs, subnets, VMs
