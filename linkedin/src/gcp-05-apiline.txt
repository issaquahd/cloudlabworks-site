 The line: one v2 call, then v4 only
 ===================================

 Below the line   POST /clusters/gcp
                  cloud.nutanix.com/api/v2
                  cluster + Prism Central, returns a task
                  the console never has to be clicked
 ---------------------------------------------------------
 Above the line   only the v4 API touches it

   pulumi package add terraform-provider nutanix/nutanix
   ntnx.VpcV2(...)      Flow VPC
   ntnx.SubnetV2(...)   overlay subnet

 Not supported on Google Cloud, unlike AWS:
   hibernate and resume, IPv6, PC backup to GCS,
   fewer than 3 or more than 28 nodes
