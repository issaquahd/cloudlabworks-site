 The line: v4 API only
 =====================

 Below the line   NC2 console builds the cluster
                  (Google Cloud says ~2 hours)
 ------------------------------------------------------
 Above the line   only the v4 API touches it

   pulumi package add terraform-provider nutanix/nutanix

   ntnx.VpcV2(...)      Flow VPC
   ntnx.SubnetV2(...)   overlay subnet
                        images, VMs, the rest

 Same v4 surface as the AWS and Azure posts.
 Same ETag safety against a colleague's console click.
 Different tool: Pulumi here, OpenTofu there.
